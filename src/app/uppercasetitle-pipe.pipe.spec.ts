import { UppercasetitlePipePipe } from './uppercasetitle-pipe.pipe';

describe('UppercasetitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UppercasetitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
