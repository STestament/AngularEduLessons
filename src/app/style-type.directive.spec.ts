import { StyleTypeDirective } from './style-type.directive';

describe('StyleTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleTypeDirective();
    expect(directive).toBeTruthy();
  });
});
