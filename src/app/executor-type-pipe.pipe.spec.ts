import { ExecutorTypePipePipe } from './executor-type-pipe.pipe';

describe('ExecutorTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExecutorTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
